#include <stdio.h>
#include <time.h>
#include <simple_logger.h>
#include "MattHelper.h"

/*void setTime(time_t currentTime)
{
	seconds = time(NULL);
	slog("Seconds since January 1, 1970 = %ld\n", seconds);
}

time_t getTime() 
{
	return seconds;
}*/